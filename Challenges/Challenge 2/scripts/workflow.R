# Workflow
# introduce the challenge title and purpose
# add group members names and contact details
# install packages (commented out)
# load libraries
# load the monthly crime in London boroughs data set
# check the data structure
# make a column for each year, summing all monthly data for each crime type per year
# sum all crime types to get total crime rate per year, for each London borough
# calculate the change in crime rate from 2011-2020, for each London borough
# produce a bar chart to show change in crime rate in different London boroughs from 2011-2020
# produce a map displaying change in crime rate in different London boroughs from 2011-2020
# reference data set